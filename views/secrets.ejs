<%- include('partials/header') %>

<div class="wrapper shadow p-5 text-center border rounded-3">
  <div class="container">
    <a href="/"><i class="fas fa-user-secret fa-6x logo"></i></a>
    <h1 class="display-6">Here are everyone's dirty secrets 😈!</h1>

    <!-- Display secrets according to current user (remember to add the beginning ejs tags) -->
    <!-- userSecrets.forEach(secret => { %>
        <p class="secret-text"> secret %></p>
     }); %> -->

    <!-- Display all secrets randomly -->
     <% usersWithSecrets.forEach((user) => { %>
      <% user.secrets.forEach((secret) => { %>
        <p class="secret-text"><%= secret %></p>
      <% }) %>
    <% }) %>
    
    <hr>
    
    <div class="d-inline-flex gap-3">
      <a class="btn btn-light btn-lg shadow-sm" href="/logout" role="button">Log Out</a>
      <a class="btn btn-dark btn-lg shadow-sm" href="/submit" role="button">Submit a Secret</a>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
